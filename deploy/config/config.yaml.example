# Middleware Deploy Kit Configuration Example

# Server Configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080

# JWT Configuration
# IMPORTANT: Generate a random secret for production
# JWT_SECRET=your-random-secret-here

# Data Directory
DATA_DIR=/opt/middleware-deploy-kit/data

# Log Level (debug, info, warn, error)
LOG_LEVEL=info
